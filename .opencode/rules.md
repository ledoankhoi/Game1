## Quy tắc
- Luôn chạy `cd game-frontend && npm run lint` sau mỗi lần sửa code frontend
- Không copy code từ tutorial/lab
- Dùng React Router DOM cho navigation, không dùng <a> tag
- API calls qua axios service, không fetch trực tiếp
- Backend: middleware pattern (auth → validation → controller)
